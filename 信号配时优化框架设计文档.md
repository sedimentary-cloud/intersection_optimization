# 单交叉口信号配时优化框架 · 设计说明文档

**版本**：v1.0
**求解后端**：scipy.optimize.milp（HiGHS）
**适用范围**：单点交叉口，相位选择 + 配时 + 相位排序；不考虑干线协调

---

## 1. 设计目标与问题定义

### 1.1 问题定义

给定一个交叉口：

- 每个进口的每个**流向**具有已知的需求流量；
- 候选**相位池**中每个相位是一个"基底"：放行若干进口的若干流向，基底中每个元素为正实数，表示该相位对相应流向的**服务能力**；
- 目标是在满足所有流向需求的前提下，求一个**无序相位组合**及其配时，并按如下优先级优化：
  1. 满足全部流量需求（可行）；
  2. 周期最小；
  3. 浪费的服务最少；
  4. 相位数量最少；
- 最后对选中的相位组合确定**相位顺序**。

### 1.2 功能需求清单

| 编号 | 需求 | 设计响应 |
|---|---|---|
| F1 | 相位选择 + 绿灯配时联合优化 | 第一阶段 MIP（§6、§8） |
| F2 | 周期可"给定"也可"最小化" | 目标管线模式开关（§8.2） |
| F3 | 通用系数线性组合约束（正、负、混合系数） | 约束描述层 + 编译层（§5、§6） |
| F4 | 约束触发逻辑：无条件 / 析取 / 合取 | Trigger 机制（§5.2） |
| F5 | 软约束（可违反，进目标惩罚） | 松弛变量机制（§5.4） |
| F6 | 相位顺序优化 | 两阶段分解，第二阶段枚举 + LP（§9） |
| F7 | 混合系数约束的退化陷阱防护 | 子集语义审计（§7） |

### 1.3 关键建模决策（设计依据）

1. **半连续结构**：未选中相位的绿灯恒为 0，通过二进制变量联动实现，而非 HiGHS 不支持的 indicator constraints；
2. **big-M 一律按物理上界推导**，禁止拍脑袋设大数（HiGHS 不支持指标约束，big-M 是唯一手段，必须用到最紧）；
3. **多目标用字典序而非加权求和**（周期、浪费、相数量纲不同，权重无法标定）；
4. **相位顺序不进 MIP**：用"保守清空时间 + 枚举重解 LP"的两阶段分解，避开槽位/顺序变量带来的 MIP 规模膨胀。

---

## 2. 总体架构

框架分六层，自下而上：

```
┌─────────────────────────────────────────────────┐
│ 6. 排序后处理层  OrderingPostProcessor           │
│    枚举排序 → 真实清空时间 → 重解 LP → 选优        │
├─────────────────────────────────────────────────┤
│ 5. 目标管线层  LexicographicPipeline             │
│    字典序多轮求解：min C → min 浪费 → min 相位数   │
├─────────────────────────────────────────────────┤
│ 4. 模型层  Stage1Model                           │
│    变量注册表 + 基础约束 + scipy milp 装配/求解    │
├─────────────────────────────────────────────────┤
│ 3. 约束编译层  ConstraintCompiler                │
│    ConstraintSpec → 线性 Row（big-M/触发/软硬）   │
│    + 子集语义审计                                 │
├─────────────────────────────────────────────────┤
│ 2. 约束描述层  ConstraintSpec / Trigger          │
│    用户面向的约束声明接口                          │
├─────────────────────────────────────────────────┤
│ 1. 数据层  Movement / Phase / IntersectionData   │
│    输入数据结构与校验                              │
└─────────────────────────────────────────────────┘
```

**层间依赖规则**：只允许上层调用下层；编译层不知道目标管线的存在；模型层不知道排序后处理的存在。每层可独立单元测试。

---

## 3. 数据层

### 3.1 核心数据结构

#### Movement（流向）

| 字段 | 类型 | 含义 |
|---|---|---|
| `mid` | str | 流向唯一标识（建议编码：`进口_流向`，如 `E_str`、`N_left`） |
| `demand` | float | 需求流量 q_m |

#### Phase（候选相位）

| 字段 | 类型 | 含义 |
|---|---|---|
| `pid` | str | 相位唯一标识 |
| `capacity` | Dict[str, float] | 基底：{流向 id: 服务能力 a_pm}，仅列出 a_pm > 0 的项 |

一个相位"放行一部分进口的一部分流向"，即 `capacity` 字典的键集合。

#### IntersectionData（路口数据）

| 字段 | 类型 | 含义 | 默认 |
|---|---|---|---|
| `movements` | Dict[str, Movement] | 全部流向 | — |
| `phases` | Dict[str, Phase] | 候选相位池 | — |
| `lost_time` | Dict[(str,str), float] | 相邻相位对 (前→后) 的清空时间 ℓ_ij | {} |
| `g_min` | float | 被选相位最小绿灯（秒） | 15 |
| `c_min` / `c_max` | float | 周期下/上界（秒） | 30 / 180 |

### 3.2 单位约定（强制）

- 流量：veh/h（需求与服务能力同单位）；
- 时间：秒；
- 服务能力约束 `Σ a_pm·g_p ≥ q_m·C` 两侧单位一致（veh/h·s），无需 3600 换算；
- **全模型数值量级应控制在 10⁰~10³**，禁止出现相差 4 个数量级以上的系数（影响 HiGHS 数值稳定性）。

### 3.3 数据校验（加载时强制检查）

1. 每个流向至少被一个候选相位服务（`capacity` 键覆盖），否则直接报错——问题必然不可行；
2. 所有 `demand > 0`、`capacity` 值 > 0；
3. `lost_time` 中引用的相位 id 必须存在于相位池；
4. `g_min < c_min ≤ c_max`；
5. 候选相位池的快速可行性预检：用最大清空时间和各流向最大服务能力做一次静态估算（见 §10.1），明显不可行时提前报错并给出原因。

### 3.4 派生量

- `l_bar = max lost_time.values()`：保守清空时间上界，第一阶段环约束使用；
- `clearance(i, j)`：查询相位对清空时间，未定义的按 0 或按配置报错（建议：配置严格模式，未定义即报错，防止漏数据）。

---

## 4. 变量层

### 4.1 变量表

| 变量 | 键 VarKey | 类型 | 界 | 说明 |
|---|---|---|---|---|
| 相位选择 | `("y", pid)` | 二进制 | [0,1] | 相位是否选中 |
| 绿灯时长 | `("g", pid)` | 连续 | [0, c_max] | 与 y_p 联动构成半连续结构 |
| 周期 | `("C", "")` | 连续 | [c_min, c_max] | 可固定可优化 |
| 软约束松弛 | `("sigma", 约束名:±)` | 连续 | [0, slack_max] | 每条软约束的每个方向一个 |

变量键统一为 `(kind, name)` 二元组，全框架通用——约束声明、目标函数、求解结果都以此寻址。

### 4.2 半连续联动结构（核心）

对每个相位 p：

```
g_min · y_p  ≤  g_p  ≤  c_max · y_p
```

- y_p = 0 ⟹ g_p = 0（未选中相位绿灯强制为零）；
- y_p = 1 ⟹ g_min ≤ g_p ≤ c_max（最小绿灯约束自动生效）。

这里的 big-M 就是 c_max——物理上界，天然最紧，无数值问题。

### 4.3 基础约束（模型层自动内置，用户无感）

| 名称 | 形式 | 说明 |
|---|---|---|
| 联动上界 | g_p − c_max·y_p ≤ 0 | ∀p |
| 联动下界 | g_p − g_min·y_p ≥ 0 | ∀p |
| 环约束 | Σ g_p + l_bar·Σ y_p − C ≤ 0 | 保守清空时间（第一阶段） |
| 服务能力 | Σ_p a_pm·g_p − q_m·C ≥ 0 | ∀m，无条件生效 |
| 至少一相 | Σ y_p ≥ 1 | 防空解 |

### 4.4 为什么不显式建模相位顺序

第一阶段不引入任何顺序变量（槽位变量 x_ik 或先后变量 z_ij）。理由：

1. 顺序变量会引入 O(n²) 二进制变量和大量弱 big-M 约束，HiGHS 剪枝能力有限，代价大；
2. 顺序对配时的影响只通过**相邻清空时间**传导，第一阶段用保守上界 l_bar 即可保证任何排序下可行；
3. 相位数 ≤ 6 时枚举排序成本极低（720 个 LP，HiGHS 解 LP 为毫秒级）。

详见 §9。

---

## 5. 约束描述层（用户接口）

### 5.1 ConstraintSpec：统一约束描述

用户的一切线性组合约束（含正负混合系数）都通过一个结构声明：

```
ConstraintSpec(
    name    = "东西直行绿信比均衡",      # 唯一名，用于日志与诊断
    coeffs  = {("g","P1"): 1.0, ("g","P3"): -0.8},   # 通用线性组合，系数可正可负
    sense   = ">=",                      # "<=" / ">=" / "=="
    rhs     = 0.0,
    trigger = Trigger.all_of("P1","P3"), # 触发逻辑，默认 always()
    soft    = False,                     # 软约束开关
    penalty = 0.0,                       # 软约束单位违反惩罚
    slack_max = None,                    # 松弛上界，默认 c_max
)
```

声明时强制校验：

- `sense` 合法；
- 软约束必须给正惩罚权重；
- 带触发的约束必须给出非空相位集合；
- `coeffs` 中引用的相位/变量必须已注册。

### 5.2 Trigger：触发逻辑三分类

| 类型 | 语义 | 构造 |
|---|---|---|
| ALWAYS | 无条件恒生效 | `Trigger.always()` |
| OR（析取） | S 中**至少一个**相位被选中时生效 | `Trigger.any_of(...)` |
| AND（合取） | S 中相位**全部被选中**时才生效 | `Trigger.all_of(...)` |

**默认规则**：混合系数（既有正又有负）的约束默认应使用 AND 触发——因为部分选中时负系数项被置零会改变约束语义，绝大多数物理场景（比例、均衡、差值类）的意图都是"全在才谈得上"。编译器遇到"混合系数 + ALWAYS/OR"的组合应发出警告并要求显式确认。

### 5.3 约束分类框架（设计的理论基础）

任何约束按**两个正交轴**分类，处理规则由分类唯一确定：

**轴一：触发逻辑** — ALWAYS / OR / AND

**轴二：约束方向与退化行为**（利用"未选中 ⟹ g=0"做退化测试：把未选中相位置零代入）

| 方向 | 全不选时退化结果 | 默认行为 |
|---|---|---|
| ≤，b ≥ 0 | 0 ≤ b，自动满足 | 无需任何处理 |
| ≥，b > 0 | 0 ≥ b，不可行 | 需 big-M 松弛，或显式接受隐式"至少选一个"语义 |
| == | 拆成两条分别判断 | 见 §6.4 |
| 混合系数 | 退化方向不确定 | 走子集审计（§7） |

**处理规则矩阵**（编译器的查表依据）：

| | ALWAYS | OR | AND |
|---|---|---|---|
| ≤（b≥0） | 直接发射 | 直接发射（自动满足） | aᵀx ≤ b + M⁺(|S|−Σy_i) |
| ≤（b<0） | 直接发射 | 逐相位复制：aᵀx ≤ b + M⁺(1−y_i) | 同上 AND 形式 |
| ≥ | 直接发射 | 逐相位复制：aᵀx ≥ b − M⁻(1−y_i) | aᵀx ≥ b − M⁻(|S|−Σy_i) |
| == | 拆两条 | 两条各按 OR 规则 | 两条各按 AND 规则 |

注：OR 触发的 ≥ 约束有一个**特例**——如果"至少选中一个相位"本来就是建模意图（如行人相位必须保留），则不加 big-M 直接发射，约束天然强制该语义。此行为必须由用户显式声明确认，编译器默认仍走 big-M 安全路径。

### 5.4 软约束机制

软约束 = 硬约束 + 违反侧松弛变量 + 目标惩罚：

- ≤ 型：`aᵀx − σ ≤ b`（σ ≥ 0 吸收超出量）
- ≥ 型：`aᵀx + σ ≥ b`
- == 型：拆两条，各配 σ⁺、σ⁻

**规则**：

1. σ 加在**违反方向**那一侧，与 big-M 项**同侧相加**（M 管"是否生效"，σ 管"生效后允许多少违反"，互不干扰）；
2. σ 必须有上界（`slack_max`，默认 c_max），防止软约束无界导致 LP 松弛崩坏；
3. 惩罚项 `penalty · σ` **常驻目标函数**，在字典序多轮求解的每一轮都保留（见 §8.4）；
4. 惩罚权重标定：σ 的量纲是秒/流量，惩罚应使"违反 1 单位软约束"的代价明显高于主目标中可接受的让步幅度。建议初值 10³ 量级，调试标准见 §8.5。

---

## 6. 约束编译层

### 6.1 职责

把 ConstraintSpec 编译成线性约束行（Row: `{VarKey: 系数} + sense + rhs`），职责包括：

1. 等式拆分（== 拆成 ≤ 和 ≥）；
2. 按 §5.3 规则矩阵选择发射形式；
3. big-M 自动推导；
4. 软约束松弛变量注册；
5. 固定选择（第二阶段）时的触发坍缩；
6. 子集语义审计（§7）。

### 6.2 big-M 推导公式（强制按此实现）

对约束 aᵀx ⩷ b，各变量取全局物理界（g、C ∈ [0, c_max]，y ∈ [0,1]）：

```
M⁺ = max(aᵀx) − b = Σ_{aᵢ>0} aᵢ·ubᵢ + Σ_{aᵢ<0} aᵢ·lbᵢ − b      （≤ 型用）
M⁻ = b − min(aᵀx) = b − Σ_{aᵢ>0} aᵢ·lbᵢ − Σ_{aᵢ<0} aᵢ·ubᵢ      （≥ 型用）
```

其中 lbᵢ/ubᵢ 取变量全局界。**实现要点**：

- 计算出的 M 若 ≤ 0，说明约束在任何触发关闭状态下都自动成立，可直接发射无松弛版本（免费紧化）；
- 在 M 上加 5% 余量（`M ← 1.05·M + ε`），防止最优解贴边时被数值误差切掉；
- **禁止**用户直接传入任意大 M；如确需覆盖，接口上要求显式确认并记录警告；
- 推导记录进日志（每条约束的实际 M 值），供调试追溯。

注意一个易错点：M 必须覆盖**触发关闭的全部状态**（含部分选中），因此对负系数项应取其下界 0（该相位可不选），而不是 g_min。不要为了收紧而使用"激活 ⟹ g ≥ g_min"推导——那只在激活侧成立，不是 M 需要支配的区域。

### 6.3 发射规则实现

以 ≤ 型、AND 触发为例，最终行形式：

```
aᵀx + M⁺·Σ_{i∈S} y_i  ≤  b + M⁺·|S|
```

- 全选中（Σy=|S|）：退化为 aᵀx ≤ b ✓
- 缺任意相位：右端多出 ≥ M⁺，约束被松弛 ✓
- 缺一个和缺三个松弛量相同（都是 M⁺），语义正确 ✓

OR 触发逐相位复制（|S| 条）：

```
aᵀx + M⁺·y_i  ≤  b + M⁺,   ∀i ∈ S
```

- 任一 y_i=1：对应那条激活（aᵀx ≤ b），其余松弛 ✓
- 全不选：全部松弛 ✓
- 比"存在性指示变量"写法少一个二进制变量，且 LP 松弛略紧 ✓

### 6.4 等式约束处理

- b = 0 且退化语义无害（如 g_i − g_j = 0 在全不选时 0=0）→ 仍需审计部分选中情形（见 §7 陷阱示例）；
- b ≠ 0 → 拆两条，各配 M⁺/M⁻；
- **设计建议**：接口文档中明确提示用户"先质疑等式本身"——信号配时中真正的物理等式很少（周期封闭除外），设计偏好类"相等"应改为容差不等式 |aᵀx − b| ≤ ε，可显著降低求解难度。

### 6.5 触发坍缩（第二阶段用）

给定固定选择 fixed_y（选中=1/未选=0）：

- 触发条件求值：AND = 全为 1；OR = 存在 1；ALWAYS = 恒真；
- 激活 → 发射普通线性行（不含 M、不含 y）；
- 未激活 → **丢弃该约束**（语义即"不生效"）。

### 6.6 软约束与 big-M 共存的正确性说明

≤ 型软约束行：`aᵀx − σ ≤ b + M·(触发关闭)`。由于 σ ≥ 0 只会**减小**左端，M 在触发关闭时仍能支配 aᵀx − σ，有效性不受影响；≥ 型对称。即松弛变量的存在不破坏 M 的推导，无需在 M 中计入 σ。

---

## 7. 子集语义审计（防退化陷阱）

### 7.1 动机

混合系数约束最危险的不是报错，而是**静默切掉可行域**。示例（绿信比均衡）：

```
g₁ − 0.8·g₂ ≥ 0
```

- 全选中：正常生效 ✓
- 只选 P1：g₁ ≥ 0，自动满足 ✓
- **只选 P2：−0.8·g₂ ≥ 0 与 g₂ ≥ 15 矛盾 ⟹ 隐式禁止了所有"含 P2 不含 P1"的方案** ✗
- 全不选：0 ≥ 0 ✓

求解器不会报 infeasible，只会安静地缩小搜索空间。这类 bug 无法靠规则完全覆盖，但可以用计算代替人工推演。

### 7.2 审计算法

对约束涉及的相位集合 S，枚举全部 2^|S| 个子集 T（|S| ≤ 4 时最多 16 种）：

```
对每个子集 T：
    lo = Σ_{i∈T, aᵢ>0} aᵢ·g_min + Σ_{i∈T, aᵢ<0} aᵢ·c_max
    hi = Σ_{i∈T, aᵢ>0} aᵢ·c_max + Σ_{i∈T, aᵢ<0} aᵢ·g_min
    （选中相位 g∈[g_min,c_max]，未选中 g=0）
    判定：
      ≤ 型： hi ≤ b → 恒满足； lo > b → 恒违反；否则 → 可能生效
      ≥ 型： lo ≥ b → 恒满足； hi < b → 恒违反；否则 → 可能生效
      == 型： 两式合并判定
```

### 7.3 审计输出与使用时机

输出表格（示例）：

| 选中子集 | 约束状态 | 预期？ |
|---|---|---|
| {P1,P2} | 可能生效 | ✓ 本意 |
| {P1} | 恒满足 | ✓ |
| {P2} | **恒违反 ← 陷阱！** | ✗ 需改 AND 触发 |
| ∅ | 恒满足 | ✓ |

**触发时机**：

1. 约束注册时自动运行（混合系数约束强制；纯非负系数可跳过）；
2. 出现"恒违反"子集时，编译器拒绝静默通过，要求用户确认触发语义；
3. 求解结果异常（不可行或解不符合直觉）时，审计报告是第一诊断工具。

约束中若含非相位变量（如 C），审计仅覆盖 g 变量部分，并在报告中标注"结论需人工复核"。

---

## 8. 目标管线层（字典序多目标）

### 8.1 为什么用字典序

三个目标——周期（秒）、浪费服务（veh/h·s）、相位数（个）——量纲不同且无可比权重。加权求和会让结果依赖难以解释的权重标定；字典序（lexicographic）逐轮求解语义清晰、可复现。

### 8.2 两种运行模式

| 模式 | 行为 |
|---|---|
| **最小化周期**（默认） | 完整三轮：min C → min 浪费 → min 相位数 |
| **给定周期** | C 固定为给定值，跳过第 1 轮，从第 2 轮开始 |

模式开关只是"是否固定 C 并跳过第 1 轮"，其余管线完全一致。

### 8.3 三轮求解流程

```
第 1 轮  min C                     → 得 C*
         （软约束惩罚项常驻目标，下同）

第 2 轮  加约束 C ≤ C*·(1+ε₁)
         min 浪费 = Σ_p Σ_m a_pm·g_p − Σ_m q_m·C    → 得 W*

第 3 轮  再加约束 浪费 ≤ W*·(1+ε₂) + δ_abs
         min Σ_p y_p
```

**实现要点**：

1. **锁定约束的容差**：ε₁、ε₂ 默认 1%~2%。ε 必须**显著大于**求解器的 `mip_rel_gap`（建议 ≥ 5 倍），否则前轮目标的次优性会在后轮被误锁成不可行；
2. **浪费的绝对容差 δ_abs**：W* 可能接近 0，纯相对容差会把后轮锁死，需加绝对容差项（默认取"全部流向 1 秒绿灯的服务量"量级）；
3. **每轮结果落盘**：各轮目标值、锁定约束右端、求解状态全部记入日志，字典序管线的可调试性完全依赖这条轨迹；
4. 任一轮不可行 → 报错并指出是哪一轮、当时累积了哪些锁定约束（见 §12 诊断）；
5. "浪费"目标中 q_m·C 项在该轮 C 已被锁定时近似常数，但保留原式以保证代码与定义一致。

### 8.4 软约束惩罚的驻留规则

惩罚项 Σ w_c·σ_c 在**每一轮**目标中都以相同权重保留。理由：字典序锁定的只是主目标，软约束的"软硬次序"不应随轮次漂移。若某轮主目标与软约束冲突（如最小相位数会加剧软约束违反），由惩罚权重决定取舍——这正是权重的语义。

### 8.5 惩罚权重标定方法

- 初值：主目标是秒级（周期），软约束违反 1 秒的代价设为 10³ 量级；
- 调试判据：
  - 解中 σ 明显大于预期 → 权重太小，加大；
  - 主目标明显劣化且 σ≈0 → 权重太大，减小；
- 每个软约束独立可调，接口上 `penalty` 按约束逐个指定。

---

## 9. 排序后处理层（两阶段分解的第二阶段）

### 9.1 输入与输出

- 输入：第一阶段选中的相位集 S\*、第 1 轮锁定的周期 C\*（或用户给定周期）、全部用户约束；
- 输出：最优相位顺序 + 该顺序下重新优化的配时；若全部排序不可行，返回失败并触发回退（§9.4）。

### 9.2 枚举 + LP 重解

对 S\* 的每个排列 order（n! 个，可先经规则过滤器裁剪）：

1. 环约束改用**真实清空时间**：

   ```
   Σ_{p∈order} g_p + Σ_k ℓ(order_k, order_{k+1}) ≤ C*   （含首尾相接）
   ```

2. 服务能力约束、联动下界（g_p ≥ g_min）照常；
3. **用户约束经触发坍缩**（§6.5）后注入：此时 y 固定，M 项消失，只剩普通线性行；
4. 目标：min 浪费（含软约束惩罚），C 固定；
5. 全部排列中取目标最优者。

此时变量全连续（纯 LP），HiGHS 求解为毫秒级；4 相位 24 个排列、6 相位 720 个排列均可接受。

### 9.3 规则过滤器

排序枚举前用纯逻辑规则裁剪（不进优化模型）：

- 安全惯例（如左转相位必须先于/后于对向直行）；
- 行人相位搭接要求（人行相位必须紧邻其搭载的机动车相位）；
- 用户自定义的合法性回调。

过滤器以纯函数形式注入：`order_filter(order) -> bool`。

### 9.4 回退机制（no-good cut）

若 S\* 的全部合法排序都不可行（第一阶段保守清空时间仍不足以覆盖真实清空时间的极端排序——罕见但可能）：

1. 向第一阶段添加割约束：`Σ_{i∈S*} y_i ≤ |S*| − 1`（禁止该相位集）；
2. 重跑第一阶段（第 2、3 轮即可，C\* 保持）；
3. 迭代至找到可行组合或证明无解。

### 9.5 两阶段方案的近似性声明（重要）

第一阶段用 l_bar = max ℓ_ij 是**保守**的：保证任何排序可行，但可能排除"真实清空时间较小、恰好能塞进更紧周期"的临界方案。缓解措施：

- 报告阶段对比 C\*（l_bar 版）与乐观版（min ℓ_ij）的差距，差距大时提示用户；
- 第二阶段 LP 中允许 C 在 [c_min, C\*] 内自由优化（而非严格固定 C\*），回收保守性损失。

---

## 10. 求解器配置与数值工程（scipy/HiGHS）

### 10.1 求解参数

| 参数 | 建议值 | 说明 |
|---|---|---|
| `time_limit` | 300 s（可调） | 单轮上限；超时但已有可行解时可用该解并告警 |
| `mip_rel_gap` | 0.005 | 工程问题 0.5% 间隙足够；与字典序 ε 协调（§8.3） |
| `presolve` | on（默认） | 保持开启 |
| `disp` | 调试时开 | 输出 HiGHS 日志 |

### 10.2 稀疏装配

- 约束矩阵用 `scipy.sparse.lil_matrix` 逐行填充，装配完成转 CSR 交给 `LinearConstraint`；
- **禁止稠密化**：相位池 × 流向数增大后稠密矩阵内存会炸；
- 变量用注册表集中管理索引（VarKey → 列号），约束行只存稀疏字典，装配时统一映射——任何"未注册变量"在装配期就报错，而不是等求解器报奇怪的结果。

### 10.3 数值尺度纪律

- 时间用秒、流量用 veh/h：系数自然落在 10⁰~10³；
- big-M 量级 = c_max（10²），与可行性容差（10⁻⁶）保持 8 个数量级差距，无"半激活"风险；
- 所有浮点比较（判断 y 是否为 1、服务是否满足）用容差 10⁻⁶~10⁻⁴，禁止与 0/1 做严格相等比较。

### 10.4 结果提取与后验校验

求解完成后必须程序化复核（不信任求解器状态字）：

1. 所有硬约束代入解验证（容差 10⁻⁴）；
2. 触发约束按实际 y 值验证"该生效的生效、该松弛的松弛"；
3. 服务能力约束逐流向验证；
4. σ 值归档，供软约束权重调参。

---

## 11. 接口设计（API 草案）

### 11.1 顶层调用

```python
# 数据组装
data = IntersectionData(movements=..., phases=..., lost_time=..., g_min=15)

# 优化器
opt = LexicographicOptimizer(data, eps_cycle=0.01, eps_waste=0.01)

# 声明约束（通用线性组合 × 触发 × 软硬）
opt.add_constraint(ConstraintSpec(
    name="东西直行绿信比均衡",
    coeffs={("g","P1"): 1.0, ("g","P3"): -0.8},
    sense=">=", rhs=0.0,
    trigger=Trigger.all_of("P1", "P3")))

opt.add_constraint(ConstraintSpec(
    name="左转合计最小绿",
    coeffs={("g","P2"): 1.0, ("g","P4"): 1.0},
    sense=">=", rhs=40.0,
    trigger=Trigger.any_of("P2", "P4"),
    soft=True, penalty=2000.0))

# 审计（可选但建议）
report = opt.audit(...)   # 子集语义审计报告

# 求解
result = opt.solve(
    fixed_cycle=None,            # 或 150.0 给定周期
    order_filter=my_filter,      # 排序规则过滤器，可选
)

# result: cycle / selected_phases / greens / waste / best_order / 每轮轨迹 / σ 值
```

### 11.2 结果对象字段

| 字段 | 含义 |
|---|---|
| `cycle` | 最终周期 |
| `selected` | 选中相位列表 |
| `greens` | {相位: 绿灯时长}（第二阶段重解后的值） |
| `order` | 最优相位顺序 |
| `waste` | 浪费的服务 |
| `sigmas` | {软约束: 违反量} |
| `rounds` | 字典序各轮的目标值与锁定记录 |
| `fallback_cuts` | 触发过的 no-good cut 次数 |

---

## 12. 不可行与异常诊断

### 12.1 不可行的分级排查顺序

1. **静态预检**（不进求解器）：每流向是否有服务相位；Σ(各流向最小流量比) + 清空时间占比是否已 > 1；
2. **字典序定位**：是哪一轮不可行——第 1 轮不可行多为需求/能力问题；第 2、3 轮不可行多为锁定容差问题（ε 与 mip_rel_gap 不协调）；
3. **约束审计**：用 §7 审计报告查混合系数约束的"恒违反"子集；
4. **松弛调试**：临时把可疑硬约束改成软约束（penalty 给大值），看 σ 落在哪条约束上——即定位冲突源。

### 12.2 常见异常模式速查

| 症状 | 最可能原因 |
|---|---|
| 解里某相位被莫名排除 | 混合系数约束退化陷阱（§7） |
| 结果随微小输入变化剧烈 | M 过大或容差不协调 |
| 第 2/3 轮报不可行 | ε < mip_rel_gap，锁定过紧 |
| 软约束完全没被尊重 | penalty 权重太小（§8.5） |
| 求解极慢 | 对称解多；检查是否有多余等价约束、相位池是否有近似重复相位 |

---

## 13. 测试策略

### 13.1 单元测试（按层）

- **编译层**：每种（触发 × 方向 × 软硬）组合各至少一个用例，断言发射的行数、系数、右端、M 值；
- **审计**：构造已知含陷阱的约束（如 §7.1 示例），断言审计报告标出"恒违反"子集；
- **数据校验**：构造缺服务流向、负需求等非法输入，断言报错。

### 13.2 集成测试（端到端）

1. **已知最优解的小算例**：手工可算的两相位路口，验证框架输出与手算一致；
2. **对称性测试**：对称路口（四进口需求相同）的输出应满足对称性；
3. **模式一致性测试**：固定周期 = 自由周期最优值时，两种模式的浪费/相位数应一致；
4. **极端值测试**：单相位、需求为 0 的流向、需求逼近通行能力上限（饱和度 → 1）。

### 13.3 回归基线

固定随机性无关的算例集（MILP 结果确定性依赖求解器版本），记录各算例的 C\*、选中相位集、浪费值，作为版本回归基线。

---

## 14. 扩展点（预留，不在 v1.0 实现）

| 扩展 | 接入位置 |
|---|---|
| 饱和度约束 ρ·Σa·g ≥ q·C | 模型层，仅改服务能力约束的一个系数 |
| 候选相位池自动生成（冲突图 → 兼容流向组合） | 数据层之前的独立模块 |
| k-of-n 触发（选中 ≥ k 个才生效） | Trigger 增加种类 + 辅助二进制变量 |
| 绿波/干线协调 | 破坏单路口假设，需重构（不在本框架路线图上） |
| 随机需求/鲁棒优化 | 数据层扩展场景集，约束按场景复制 |

---

## 附录 A. 符号表

| 符号 | 含义 |
|---|---|
| q_m | 流向 m 的需求流量 |
| a_pm | 相位 p 对流向 m 的服务能力（基底元素，>0） |
| y_p | 相位 p 是否选中（0/1） |
| g_p | 相位 p 的绿灯时长（秒） |
| C | 信号周期（秒） |
| ℓ_ij / l_bar | 相邻相位对清空时间 / 其最大值（保守上界） |
| S | 约束触发涉及的相位集合 |
| M⁺ / M⁻ | ≤ 型 / ≥ 型约束的 big-M 松弛量 |
| σ | 软约束松弛变量 |
| ε₁, ε₂, δ_abs | 字典序锁定容差（相对 / 相对 / 绝对） |

## 附录 B. 一页纸速查

**建模口诀**：
1. 未选中相位 g=0，靠 `g_min·y ≤ g ≤ c_max·y` 联动实现；
2. 上界型约束（≤, b≥0）不处理；下界型看语义；等式拆两边；
3. 混合系数约束默认 AND 触发 + 子集审计；
4. M 按物理上界推导，乘 1.05 余量，禁止拍大数；
5. 软约束 = 违反侧加 σ，惩罚常驻每轮目标；
6. 多目标走字典序，ε 必须大于 mip_rel_gap；
7. 顺序不进 MIP：第一阶段保守清空时间，第二阶段枚举排序 + LP 重解；
8. 任何约束进模型前先过退化测试：置零代入，三种归宿（自动满足/自动违反/语义错误）对号入座。
