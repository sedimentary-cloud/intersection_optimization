# 单交叉口信号配时优化算法库 · 使用说明与原理手册

版本：v1.0  
求解后端：`scipy.optimize.milp`（HiGHS）  
对应设计文档：`信号配时优化框架设计文档.md`  
实现代码：`signal_timing/`

---

## 目录

1. [快速开始](#1-快速开始)
2. [算法计算原理](#2-算法计算原理)
3. [数据建模](#3-数据建模)
4. [约束配置详解](#4-约束配置详解)
5. [顶层 API 使用说明](#5-顶层-api-使用说明)
6. [求解器与数值参数](#6-求解器与数值参数)
7. [性能指导](#7-性能指导)
8. [常见陷阱与排查](#8-常见陷阱与排查)
9. [可视化与结果解读](#9-可视化与结果解读)
10. [完整案例](#10-完整案例)
11. [测试与复现](#11-测试与复现)
12. [扩展点与已知限制](#12-扩展点与已知限制)
13. [附录：符号表与 API 速查](#13-附录符号表与-api-速查)

---

## 1. 快速开始

### 1.1 环境

推荐使用项目配套的 conda 环境：

```bash
/home/qktx/artery_milp/conda-envs/artery_milp/bin/python
```

环境信息：

- Python 3.11.16
- numpy 2.4.6
- scipy 1.17.1
- matplotlib 3.11.1

检查：

```bash
/home/qktx/artery_milp/conda-envs/artery_milp/bin/python -c "import scipy; from scipy.optimize import milp; print(scipy.__version__, 'milp ok')"
```

### 1.2 最小示例

```python
from signal_timing import (
    IntersectionData, Movement, Phase,
    LexicographicOptimizer, ConstraintSpec, Trigger,
)

# 1) 数据层：流向 + 候选相位
data = IntersectionData(
    movements={
        "E_str": Movement("E_str", 600.0),
        "W_str": Movement("W_str", 600.0),
    },
    phases={
        "P_E": Phase("P_E", {"E_str": 1800.0}),
        "P_W": Phase("P_W", {"W_str": 1800.0}),
    },
    lost_time={("P_E", "P_W"): 5.0, ("P_W", "P_E"): 5.0},
    g_min=15.0,
    c_min=30.0,
    c_max=180.0,
)

# 2) 优化器
opt = LexicographicOptimizer(data, mip_rel_gap=0.001)

# 3) 可选：声明用户约束
opt.add_constraint(ConstraintSpec(
    name="E_W_绿信比均衡",
    coeffs={("g", "P_E"): 1.0, ("g", "P_W"): -0.8},
    sense=">=",
    rhs=0.0,
    trigger=Trigger.all_of("P_E", "P_W"),
))

# 4) 求解
result = opt.solve()

print("周期 C =", result.cycle)
print("选中相位 =", result.selected)
print("配时 =", result.greens)
print("相位顺序 =", result.order)
print("浪费 =", result.waste)
print("软约束违反 =", result.sigmas)
print("后验校验 =", result.verification)
```

### 1.3 结果对象字段

| 字段 | 含义 |
|---|---|
| `cycle` | 最终周期 C（秒） |
| `selected` | 最终选中相位列表 |
| `greens` | {相位: 绿灯秒数} |
| `order` | 最优相位顺序；`run_ordering=False` 时为 `None` |
| `waste` | 浪费服务量（veh/h·s） |
| `sigmas` | 各软约束的违反量 |
| `rounds` | 字典序各轮轨迹 |
| `fallback_cuts` | no-good cut 回退次数 |
| `stage1_cycle` | 第一阶段最终周期 |
| `stage1_waste` | 第一阶段最终浪费 |
| `audit_reports` | 注册约束时的审计报告 |
| `verification` | 后验校验结果字典 |
| `status` / `message` | 求解状态与说明 |

---

## 2. 算法计算原理

### 2.1 问题定义

给定：

- 流向集合 `M`，每个流向 `m` 有需求 `q_m`（veh/h）；
- 候选相位池 `P`，每个相位 `p` 的基底 `capacity[p][m] = a_pm`，表示相位 `p` 放行时对流向 `m` 的服务能力；
- 相邻相位清空时间 `ℓ(i,j)`（秒）；
- 最小绿灯 `g_min`、周期上下界 `[c_min, c_max]`；
- 用户自定义线性约束集合。

要求：

1. 选择相位集合；
2. 给每个选中相位分配绿灯时长；
3. 决定周期；
4. 决定相位顺序；
5. 满足所有流向需求；
6. 按字典序优化：
   - 第一目标：`min C`；
   - 第二目标：`min 浪费`；
   - 第三目标：`min 相位数`。

### 2.2 第一阶段：无序相位选择 + 配时 MILP

#### 变量

| 变量 | VarKey | 类型 | 界 |
|---|---|---|---|
| 相位选择 | `("y", pid)` | 二进制 | `[0, 1]` |
| 绿灯时长 | `("g", pid)` | 连续 | `[0, c_max]` |
| 周期 | `("C", "")` | 连续 | `[c_min, c_max]` 或固定 |
| 软约束松弛 | `("sigma", 约束名:±)` | 连续 | `[0, slack_max]` |

#### 基础约束（模型层自动内置）

1. **半连续联动**

```text
g_min · y_p ≤ g_p ≤ c_max · y_p
```

等价于两条线性约束：

```text
g_p − c_max · y_p ≤ 0
g_p − g_min · y_p ≥ 0
```

意义：未选中相位绿灯恒为 0；选中相位绿灯至少 `g_min`。

2. **第一阶段环约束（保守清空时间）**

```text
Σ_p g_p + l_bar · Σ_p y_p − C ≤ 0
```

其中：

```text
l_bar = max_{i,j} ℓ(i,j)
```

意义：用最大清空时间给每个选中相位预留清空时间，保证第一阶段选出的相位集合对任意顺序都可行。

3. **服务能力约束**

```text
Σ_p a_pm · g_p − q_m · C ≥ 0     ∀m
```

意义：周期内相位 p 对流向 m 的服务量 `a_pm · g_p`（单位 veh/h·s）必须不少于需求 `q_m · C`。

4. **至少一相**

```text
Σ_p y_p ≥ 1
```

意义：防止空解。

#### 第一阶段求解目标

```text
min C + Σ_c penalty_c · σ_c
```

软约束惩罚项在每一轮目标中都保留。

### 2.3 用户约束编译层

用户约束统一表示为：

```python
ConstraintSpec(
    name="约束名",
    coeffs={("g","P1"): 1.0, ("g","P2"): -0.8, ("C",""): 0.1},
    sense=">=",
    rhs=0.0,
    trigger=Trigger.all_of("P1", "P2"),
    soft=False,
    penalty=0.0,
    slack_max=None,
)
```

编译层把它转换为线性行，并自动处理：

- 等式拆分；
- big-M 松弛；
- 触发逻辑；
- 软约束松弛变量；
- 第二阶段触发坍缩。

#### big-M 推导

对约束 `aᵀx ⋚ b`，按物理界推导最紧的 M：

```text
M⁺ = max(aᵀx) − b
   = Σ_{a_i>0} a_i·ub_i + Σ_{a_i<0} a_i·lb_i − b      （≤ 型用）

M⁻ = b − min(aᵀx)
   = b − Σ_{a_i>0} a_i·lb_i − Σ_{a_i<0} a_i·ub_i      （≥ 型用）
```

物理界：

- `g, C ∈ [0, c_max]`；
- `y ∈ [0, 1]`。

若 `M ≤ 0`，说明约束在任何触发状态下都自动成立，直接发射无松弛版本。

否则加 5% + ε 余量：

```text
M ← 1.05 · M + 1e-6
```

#### 触发规则矩阵

`S` 为触发涉及的相位集合，`k = |S|`。

**≤ 型：**

| 触发 | b ≥ 0 | b < 0 |
|---|---|---|
| ALWAYS | 直接发射 | 直接发射 |
| OR | 直接发射（关闭态 0 ≤ b 自动满足） | 逐相位复制：`aᵀx + M⁺·y_i ≤ b + M⁺` |
| AND | `aᵀx + M⁺·Σ_{i∈S} y_i ≤ b + M⁺·k` | 同左 |

**≥ 型：**

| 触发 | 发射形式 |
|---|---|
| ALWAYS | 直接发射 |
| OR | 逐相位复制：`aᵀx − M⁻·y_i ≥ b − M⁻` |
| AND | `aᵀx − M⁻·Σ_{i∈S} y_i ≥ b − M⁻·k` |

**== 型：**

拆成 `≤` 和 `≥` 两条，分别按上表处理。

#### OR ≥ 的存在性特例

如果 OR 触发 + `≥` 的本来意图就是“至少选中一个相位”，可设置：

```python
ConstraintSpec(..., trigger=Trigger.any_of("P1","P2"), or_existential=True)
```

此时不加大 M，直接发射，约束天然强制“至少选一个”。

### 2.4 子集语义审计

混合系数约束最危险的问题不是报错，而是**静默砍掉可行域**。

例如：

```text
g1 − 0.8·g2 ≥ 0
```

若触发为 ALWAYS：

- 只选 P1：`g1 ≥ 0`，自动满足；
- 只选 P2：`−0.8·g2 ≥ 0` 与 `g2 ≥ g_min` 矛盾；
- 结果：所有“含 P2 不含 P1”的方案被隐式禁止。

审计模块对约束涉及的相位集合枚举全部 `2^|S|` 个子集：

```text
对每个子集 T：
    lo = Σ_{i∈T, a_i>0} a_i·g_min + Σ_{i∈T, a_i<0} a_i·c_max
    hi = Σ_{i∈T, a_i>0} a_i·c_max + Σ_{i∈T, a_i<0} a_i·g_min
```

判定：

- `≤`：`hi ≤ b` 恒满足；`lo > b` 恒违反；否则可能生效；
- `≥`：`lo ≥ b` 恒满足；`hi < b` 恒违反；否则可能生效；
- `==`：区间与 b 的关系合并判定。

输出会标记：

- 恒满足子集；
- 恒违反子集；
- 是否会触发；
- 是否是陷阱。

规则：

- 空集恒违反由模型内置 `Σy ≥ 1` 兜底，不视为陷阱；
- 软约束的恒违反只意味着必须付惩罚，不视为硬陷阱；
- 含 `C`、`y` 等非 g 变量时，审计只覆盖 g 部分，并标记“需人工复核”；
- 发现硬约束陷阱时，默认拒绝注册，除非用户显式确认。

### 2.5 字典序多目标管线

#### 第 1 轮：最小化周期

```text
min C + Σ penalty·σ
```

得到 `C*`。

若用户给定周期 `fixed_cycle`，跳过第 1 轮，直接令 `C* = fixed_cycle`。

#### 第 2 轮：最小化浪费

先加入锁定约束：

```text
C ≤ C* · (1 + eps_cycle)
```

然后：

```text
min Σ_p Σ_m a_pm·g_p − Σ_m q_m·C + Σ penalty·σ
```

得到 `W*`。

#### 第 3 轮：最小化相位数

先加入锁定约束：

```text
浪费 ≤ W* · (1 + eps_waste) + delta_abs
```

然后：

```text
min Σ_p y_p + Σ penalty·σ
```

最终解给出选中相位、绿灯、周期和浪费。

#### `delta_abs` 默认值

```text
delta_abs = Σ_m max_p a_pm
```

即“全部流向各 1 秒最大服务量之和”。

### 2.6 第二阶段：排序后处理

第一阶段不引入任何顺序变量，因此得到的是一组无序相位。

第二阶段 `OrderingPostProcessor`：

1. 取第一阶段选中集合 `S*`；
2. 排序后枚举 `S*` 的全部 `n!` 个排列；
3. 可选 `order_filter(order)` 过滤；
4. 对每个排列构造 LP：
   - 环约束改成真实清空时间：

```text
Σ_i g_{p_i} + Σ_i ℓ(p_i, p_{i+1 mod n}) ≤ C
```

   - 服务能力约束照常；
   - 用户约束触发坍缩后注入；
   - 目标：

```text
min Σ_p Σ_m a_pm·g_p − Σ_m q_m·C + Σ penalty·σ
```

5. 取所有可行顺序中目标最小的一个；
6. 平局时保留先枚举到的顺序（倾向于字典序较小的顺序）。

周期处理：

- `fixed_cycle` 给定：C 固定；
- 自由模式 + `allow_cycle_reduction=True`：`C ∈ [c_min, C*]`，允许利用真实清空时间回收第一阶段的保守损失；
- 自由模式 + `allow_cycle_reduction=False`：C 固定在第一阶段解。

### 2.7 no-good cut 回退

如果当前相位集的所有合法顺序都不可行，添加：

```text
Σ_{i∈S*} y_i ≤ |S*| − 1
```

禁止该相位集，重新跑第一阶段（自由模式保持 C*，固定周期模式保持给定周期），再排序。

最多回退 `max_fallback` 次。

### 2.8 后验校验

求解完成后程序化复核：

1. 环约束（真实清空时间）；
2. 服务能力逐流向；
3. 半连续上下界；
4. 用户约束逐行代入；
5. σ 值归档。

默认容差 `1e-5`。不通过会抛出 `PostCheckError`。

---

## 3. 数据建模

### 3.1 Movement

```python
Movement(mid="E_left", demand=200.0)
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `mid` | str | 流向唯一 id |
| `demand` | float | 需求流量，veh/h，必须 > 0 |

### 3.2 Phase

```python
Phase(
    pid="P1_NS_TH",
    capacity={"N_thr": 1800.0, "S_thr": 1800.0},
)
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `pid` | str | 相位唯一 id |
| `capacity` | Dict[str, float] | {流向 id: 服务能力 a_pm}，只列 a_pm > 0 的项 |

候选相位的语义：

- 一个相位是“绿灯期间同时放行哪些流向”的基底；
- `capacity` 表示每个被放行流向的服务能力；
- 框架不检查交通冲突，**候选相位必须是用户认为物理可行的放行组合**。

### 3.3 IntersectionData

```python
IntersectionData(
    movements={...},
    phases={...},
    lost_time={("P1","P2"): 3.0, ...},
    g_min=11.0,
    c_min=40.0,
    c_max=180.0,
    strict_clearance=True,
    static_precheck=True,
)
```

| 字段 | 默认 | 说明 |
|---|---|---|
| `movements` | — | 流向字典 |
| `phases` | — | 候选相位池 |
| `lost_time` | `{}` | 相邻相位对 (前→后) 清空时间 |
| `g_min` | 15 | 选中相位最小绿灯 |
| `c_min` / `c_max` | 30 / 180 | 周期上下界 |
| `strict_clearance` | True | 查询未定义清空时间时是否报错 |
| `static_precheck` | True | 是否启用静态可行性预检 |

### 3.4 加载时校验

`IntersectionData` 会强制检查：

1. 每个流向至少被一个候选相位服务；
2. 所有需求 > 0；
3. 所有 capacity 值 > 0；
4. capacity 中引用的流向必须存在；
5. `lost_time` 中引用的相位必须存在；
6. `lost_time` 值 ≥ 0；
7. `g_min < c_min ≤ c_max`；
8. 静态预检：
   - 每个流向的需求不能达到/超过其最大服务能力；
   - 用 `l_bar` 和关键流量比估算最小周期，明显超过 `c_max` 时提前报错。

### 3.5 单位约定

| 量 | 单位 |
|---|---|
| 流量 | veh/h |
| 服务能力 | veh/h |
| 时间、绿灯、周期、清空 | 秒 |
| 服务量 | veh/h·s |
| 需求侧 `q_m · C` | veh/h·s |

约束中不需要 3600 换算；只有在绘图/统计车辆数时才用 `/3600`。

### 3.6 派生量

| 属性 | 含义 |
|---|---|
| `l_bar` | max lost_time，第一阶段保守清空时间 |
| `l_min` | min lost_time，固定周期模式乐观回退用 |
| `clearance(i,j)` | 查询相邻清空时间；严格模式未定义则报错 |
| `order_clearance(order)` | 一个完整顺序的首尾相接清空时间 |
| `max_capacity(mid)` | 流向 max_p a_pm |
| `service_phase_ids(mid)` | 服务该流向的相位列表 |

---

## 4. 约束配置详解

### 4.1 变量键 VarKey

统一使用 `(kind, name)`：

| kind | name | 含义 |
|---|---|---|
| `"y"` | 相位 id | 相位是否选中，0/1 |
| `"g"` | 相位 id | 绿灯时长 |
| `"C"` | `""` | 周期 |
| `"sigma"` | `约束名:±` | 软约束松弛 |

例如：

```python
("g", "P1_NS_TH")
("y", "P1_NS_TH")
("C", "")
```

### 4.2 ConstraintSpec 字段

```python
ConstraintSpec(
    name="约束唯一名",
    coeffs={("g","P1"): 1.0, ("g","P2"): -0.8},
    sense=">=",
    rhs=0.0,
    trigger=Trigger.all_of("P1", "P2"),
    soft=False,
    penalty=0.0,
    slack_max=None,
    confirm_mixed_trigger=False,
    confirm_audit=False,
    or_existential=False,
)
```

| 字段 | 说明 |
|---|---|
| `name` | 唯一名，用于审计、日志和 σ 命名 |
| `coeffs` | `{VarKey: 系数}`，系数可正可负 |
| `sense` | `"<="`、`">="`、`"=="` |
| `rhs` | 右端常数 |
| `trigger` | ALWAYS / OR / AND 触发 |
| `soft` | 是否软约束 |
| `penalty` | 软约束单位违反惩罚，必须 > 0 |
| `slack_max` | σ 上界，默认 `c_max` |
| `confirm_mixed_trigger` | 混合系数 + 非 AND 触发的显式确认 |
| `confirm_audit` | 审计发现恒违反子集时的显式确认 |
| `or_existential` | OR + ≥ 的“至少选一个”语义，直接发射 |

### 4.3 Trigger 三分类

#### ALWAYS

```python
trigger=Trigger.always()
```

无条件恒生效。

#### OR（析取）

```python
trigger=Trigger.any_of("P1", "P2")
```

S 中至少一个相位被选中时生效。

#### AND（合取）

```python
trigger=Trigger.all_of("P1", "P2")
```

S 中所有相位都被选中时才生效。

**默认建议**：混合系数约束使用 AND 触发，避免部分选中时负系数项被置零改变语义。

### 4.4 约束方向的含义

| sense | 含义 | 典型用途 |
|---|---|---|
| `<=` | 上界 | 绿灯不超过某值、相位差不超过某值、相位数上限 |
| `>=` | 下界 | 最小绿、绿信比下限、服务能力下限 |
| `==` | 等式 | 真正有物理意义的相等关系；尽量少用 |

设计文档建议：信号配时中真正的物理等式很少，设计偏好类“相等”应改成容差不等式，例如：

```text
|g1 − g2| ≤ 5
```

写成两条 ≤ 约束，比 `g1 − g2 == 0` 更容易求解、更不容易产生退化陷阱。

### 4.5 软约束

软约束 = 硬约束 + 违反侧 σ + 目标惩罚。

- `≤` 型：`aᵀx − σ ≤ b`
- `≥` 型：`aᵀx + σ ≥ b`
- `==` 型：拆两条，分别配 `σ⁺`、`σ⁻`

规则：

1. σ 有上界 `slack_max`，默认 `c_max`；
2. 惩罚项常驻字典序每一轮；
3. 惩罚太小 → σ 被大量使用，软约束形同虚设；
4. 惩罚太大 → 主目标（周期/浪费/相位数）被显著牺牲；
5. 建议初值 `1e3` 量级，再按调试判据调整。

### 4.6 常用约束模板

#### 1. 某相位最小绿

```python
ConstraintSpec(
    name="P1 最小绿 20s",
    coeffs={("g","P1"): 1.0},
    sense=">=",
    rhs=20.0,
    trigger=Trigger.any_of("P1"),   # 只在 P1 选中时生效
)
```

如果想表达“P1 必须被选中”，可使用 `or_existential` 或加 `y` 约束。

#### 2. 两个相位绿信比均衡

```python
ConstraintSpec(
    name="P1/P2 绿信比均衡",
    coeffs={("g","P1"): 1.0, ("g","P2"): -0.8},
    sense=">=",
    rhs=0.0,
    trigger=Trigger.all_of("P1","P2"),
)
```

#### 3. 两个相位绿灯差不超过 5s

```python
ConstraintSpec(
    name="P1-P2 差值上界",
    coeffs={("g","P1"): 1.0, ("g","P2"): -1.0},
    sense="<=",
    rhs=5.0,
    trigger=Trigger.all_of("P1","P2"),
)
ConstraintSpec(
    name="P2-P1 差值上界",
    coeffs={("g","P2"): 1.0, ("g","P1"): -1.0},
    sense="<=",
    rhs=5.0,
    trigger=Trigger.all_of("P1","P2"),
)
```

#### 4. 左转合计最小绿

```python
ConstraintSpec(
    name="左转合计最小绿",
    coeffs={("g","P2"): 1.0, ("g","P4"): 1.0},
    sense=">=",
    rhs=40.0,
    trigger=Trigger.any_of("P2","P4"),
    soft=True,
    penalty=2000.0,
)
```

#### 5. 相位数上限

```python
ConstraintSpec(
    name="最多选 4 个相位",
    coeffs={("y","P1"): 1.0, ("y","P2"): 1.0, ("y","P3"): 1.0, ("y","P4"): 1.0},
    sense="<=",
    rhs=4.0,
)
```

#### 6. 某相位必须选中

```python
ConstraintSpec(
    name="P2 必选",
    coeffs={("y","P2"): 1.0},
    sense=">=",
    rhs=1.0,
)
```

#### 7. 某相位禁止选中

```python
ConstraintSpec(
    name="P2 禁用",
    coeffs={("y","P2"): 1.0},
    sense="<=",
    rhs=0.0,
)
```

#### 8. 周期上界/下界

```python
ConstraintSpec(
    name="周期不超过 120s",
    coeffs={("C",""): 1.0},
    sense="<=",
    rhs=120.0,
)
ConstraintSpec(
    name="周期至少 60s",
    coeffs={("C",""): 1.0},
    sense=">=",
    rhs=60.0,
)
```

#### 9. 服务能力/饱和度约束

```python
# Σ a_pm·g_p ≥ ρ · q_m · C
ConstraintSpec(
    name="E 流向饱和度 0.9",
    coeffs={("g","P1"): 1800.0, ("C",""): -0.9*600.0},
    sense=">=",
    rhs=0.0,
)
```

#### 10. OR ≥ 存在性语义

```python
ConstraintSpec(
    name="至少一个左转相位被选中",
    coeffs={("g","P2"): 1.0, ("g","P4"): 1.0},
    sense=">=",
    rhs=40.0,
    trigger=Trigger.any_of("P2","P4"),
    or_existential=True,
)
```

### 4.7 审计报告怎么读

```python
report = opt.audit(spec)
print(report.as_table())
```

典型输出：

```text
选中子集 | 状态 | 是否触发 | 陷阱?
    {P1} | 恒满足 | False | 否
    {P2} | 恒违反 | False | 否
{P1,P2} | 可能生效 | True | 否
```

- `恒满足`：该子集下约束一定成立；
- `恒违反`：该子集下约束一定不成立；
- `可能生效`：取决于具体 g 值；
- `陷阱 = 是`：触发激活且恒违反，说明该约束会静默禁止某些相位组合；
- `需人工复核`：约束含非 g 变量，区间结论只覆盖 g 部分。

---

## 5. 顶层 API 使用说明

### 5.1 构造优化器

```python
opt = LexicographicOptimizer(
    data,
    eps_cycle=0.01,
    eps_waste=0.01,
    delta_abs=None,
    mip_rel_gap=0.005,
    time_limit=300.0,
    disp=False,
    max_fallback=20,
)
```

| 参数 | 默认 | 说明 |
|---|---|---|
| `eps_cycle` | 0.01 | 第 2 轮周期锁定相对容差 |
| `eps_waste` | 0.01 | 第 3 轮浪费锁定相对容差 |
| `delta_abs` | None | 浪费绝对容差；默认 `Σ_m max_p a_pm` |
| `mip_rel_gap` | 0.005 | MILP 相对间隙 |
| `time_limit` | 300 | 单轮/单个 LP 时间上限（秒） |
| `disp` | False | 是否输出 HiGHS 日志 |
| `max_fallback` | 20 | 排序 no-good cut 最大回退次数 |

### 5.2 声明约束

```python
opt.add_constraint(spec)
opt.add_constraints([spec1, spec2])
```

注册时会：

1. 校验 `ConstraintSpec`；
2. 检查约束名是否重复；
3. 运行子集审计（可选 `run_audit=False` 关闭）；
4. 发现硬陷阱时抛出 `ConstraintAuditError`，除非显式确认。

### 5.3 审计

```python
report = opt.audit(spec)      # 现场审计一条约束
reports = opt.audit()          # 返回已注册约束的审计报告列表
```

### 5.4 求解

```python
result = opt.solve(
    fixed_cycle=None,              # 或 120.0
    order_filter=None,             # 顺序过滤器
    run_ordering=True,             # 是否运行第二阶段排序
    allow_cycle_reduction=True,    # 第二阶段是否允许 C ∈ [c_min, C*]
)
```

#### `fixed_cycle`

- `None`：自由周期，运行完整字典序；
- 数值：固定周期模式，跳过第 1 轮，从第 2 轮开始。

固定周期模式如果第一阶段保守 `l_bar` 不可行，会：

1. 用乐观 `l_min` 重试第一阶段；
2. 第二阶段用真实清空时间验证顺序；
3. 若所有顺序仍不可行，触发 no-good cut 回退。

#### `order_filter`

```python
def my_filter(order: tuple[str, ...]) -> bool:
    # 返回 False 表示该顺序不合法
    return True

result = opt.solve(order_filter=my_filter)
```

用途：

- 安全惯例：左转相位必须先于/后于对向直行；
- 行人搭接要求：行人相位必须紧邻搭载的机动车相位；
- 用户自定义合法性回调。

#### `run_ordering`

- `True`（默认）：运行第二阶段，得到最优顺序和重解后的配时；
- `False`：跳过排序，结果 `order=None`，使用第一阶段保守环约束下的配时。

#### `allow_cycle_reduction`

- `True`（默认）：第二阶段允许 `C ∈ [c_min, C*]`，回收第一阶段保守清空时间造成的周期损失；
- `False`：第二阶段周期固定为第一阶段最终周期，结果更保守。

### 5.5 异常类型

| 异常 | 触发场景 |
|---|---|
| `DataValidationError` | 数据加载校验失败 |
| `ConstraintError` | 约束描述/编译错误 |
| `ConstraintAuditError` | 审计发现硬陷阱且未确认 |
| `InfeasibleError` | 某一轮 MILP 或回退阶段不可行 |
| `OrderingError` | 排序层不可恢复错误 |
| `PostCheckError` | 求解完成后后验校验失败 |
| `CollapsedConstraintInfeasible` | 触发坍缩后约束退化为恒违反常数行 |

---

## 6. 求解器与数值参数

### 6.1 字典序容差

`eps_cycle` 必须显著大于 `mip_rel_gap`：

```text
eps_cycle ≥ 5 · mip_rel_gap     （建议）
```

如果 `eps_cycle < mip_rel_gap`，程序会直接报错；如果小于 5 倍，会给出警告。

原因：第 1 轮的 MILP 有 `mip_rel_gap` 的相对最优性间隙，如果第 2 轮锁定过紧，会把第 1 轮的次优解误锁成不可行。

### 6.2 `delta_abs`

`delta_abs` 用于防止 `W*` 接近 0 时相对容差把第 3 轮锁死。

默认：

```text
delta_abs = Σ_m max_p a_pm
```

即全部流向各 1 秒最大服务量之和。它越大，第 3 轮浪费锁定越松，相位数优化越自由；它越小，相位数优化越严格，但可能因数值问题不可行。

### 6.3 big-M 余量

固定为：

```text
M ← 1.05·M + 1e-6
```

- 5% 余量防止最优解贴边时被数值误差切掉；
- `1e-6` 防止极小 M 直接变成 0；
- `M ≤ 0` 时直接发射无松弛版本。

### 6.4 数值尺度纪律

- 时间用秒、流量用 veh/h；
- 系数自然落在 `10^0 ~ 10^3`；
- big-M 量级约 `c_max`（10²）；
- 可行性容差约 `10^-6`；
- 禁止出现相差 4 个数量级以上的系数。

### 6.5 结果容差

后验校验默认容差 `1e-5`。求解器本身的可行性容差会略小；如果发现后验校验失败但违反量很小，优先检查：

- big-M 是否过小；
- 服务约束系数是否尺度差异过大；
- `g_min`、`c_min`、`c_max` 是否设置合理。

---

## 7. 性能指导

### 7.1 模型规模

第一阶段 MILP：

| 项 | 规模 |
|---|---|
| 变量 | O(|P| + 相位数 + 软约束数) |
| 基础约束 | O(|P| + |M|) |
| 用户约束行 | 取决于触发：AND 1 行，OR 最多 |S| 行，== 再乘 2 |
| 二进制变量 | |P| 个 y |

第二阶段排序：

| 项 | 规模 |
|---|---|
| 排列数 | 选中相位数 n! |
| 每个排列 | 一个纯 LP |
| 变量 | n 个 g + 1 个 C + σ |
| 约束 | 1 条环 + |M| 条服务 + 用户约束 |

### 7.2 性能建议

1. **控制候选相位池大小**
   - 排序是 n!，选中相位数建议 ≤ 6；
   - 6 相位 720 个 LP，HiGHS 通常毫秒级；
   - 7 相位 5040 个 LP，8 相位 40320 个 LP，会明显变慢。

2. **用 `order_filter` 剪枝**
   - 安全惯例、行人搭接、工程经验都能提前排除大量顺序；
   - 过滤器是纯函数，不会进入优化模型。

3. **避免近似重复的候选相位**
   - 对称或近似重复的相位会导致大量等价解，减慢 MILP；
   - 候选相位池应尽量精简、互相区分。

4. **优先用不等式而不是等式**
   - 等式会拆成两条约束，增加模型规模；
   - 设计偏好用容差不等式更稳定。

5. **软约束惩罚要标定**
   - 太小：软约束被大量违反，但求解快；
   - 太大：主目标被牺牲，MILP 也更难；
   - 建议从 `1e3` 开始，根据 σ 和主目标变化调整。

6. **固定周期模式做批量实验**
   - 如果只需要比较相位方案，固定周期模式跳过第 1 轮，快很多；
   - 也避免 `eps_cycle` 和 `mip_rel_gap` 的耦合。

7. **设置合理 `time_limit`**
   - 默认 300s；
   - 工程问题通常远小于这个值；
   - 超时但有可行解时，程序会告警并使用该可行解。

8. **保持数值尺度**
   - 不要把秒写成毫秒、不要混用 veh/h 和 veh/s；
   - 不要让某条约束系数特别大或特别小。

9. **利用对称性认识**
   - 对称路口有多个等价最优解，结果可能随求解器版本变化；
   - 如果需要固定某个解，可加最小约束或 `order_filter`。

### 7.3 复杂度速览

| 场景 | 第一阶段 | 第二阶段 |
|---|---|---|
| 2 相位 | 毫秒级 | 2 个 LP |
| 4 相位 | 毫秒级 | 24 个 LP |
| 6 相位 | 毫秒~秒级 | 720 个 LP |
| 8 相位 | 取决于约束 | 40320 个 LP，慎用 |

---

## 8. 常见陷阱与排查

### 8.1 混合系数退化陷阱

**症状**：解里某个相位被莫名排除，或某些明明合理的相位组合永远不出现。

**原因**：混合系数约束 + ALWAYS/OR 触发，负系数项在部分选中时被置零，改变语义。

**处理**：

- 默认使用 `Trigger.all_of(...)`；
- 注册时查看审计报告；
- 发现 `陷阱 = 是` 时，修改触发或显式 `confirm_audit=True`。

### 8.2 软约束惩罚太小

**症状**：σ 很大，软约束几乎没被尊重。

**处理**：增大 `penalty`，直到 σ 回到可接受范围。

### 8.3 软约束惩罚太大

**症状**：σ≈0，但周期、浪费或相位数明显劣化。

**处理**：减小 `penalty`，观察主目标改善。

### 8.4 `eps_cycle` 与 `mip_rel_gap` 不协调

**症状**：第 2 轮/第 3 轮报不可行。

**原因**：锁定约束过紧，前轮次优解被误锁成不可行。

**处理**：

- 增大 `eps_cycle` / `eps_waste`；
- 或减小 `mip_rel_gap`；
- 建议 `eps ≥ 5 · mip_rel_gap`。

### 8.5 `delta_abs` 过大或过小

**症状**：相位数没有被真正最小化，或第 3 轮不可行。

**处理**：

- 过大：减小 `delta_abs`；
- 过小且不可行：增大 `delta_abs`。

### 8.6 浪费目标导致周期偏大

**现象**：在第 2 轮允许 `C ≤ C*(1+eps_cycle)` 时，如果服务能力过剩，增大 C 可能降低“每周期浪费”，于是解偏向周期上界。

**处理**：

- 减小 `eps_cycle`；
- 设置 `allow_cycle_reduction=False`；
- 或把“浪费”改成其他目标/增加周期项惩罚。

### 8.7 第一阶段保守 `l_bar` 导致周期偏大

**原因**：第一阶段用最大清空时间给每个选中相位预留，可能过保守。

**处理**：

- 开启 `allow_cycle_reduction=True`（默认），第二阶段用真实清空时间回收；
- 或使用固定周期模式，程序会在必要时用 `l_min` 乐观重试。

### 8.8 固定周期模式不可行

**原因**：给定周期小于第一阶段保守 `l_bar` 所需时间。

**处理**：

- 程序会自动用 `l_min` 重试；
- 如果真实顺序仍不可行，会走 no-good cut；
- 仍失败说明该周期确实不可行，需要增大周期或减少相位需求。

### 8.9 清空时间未定义

**症状**：排序阶段抛 `DataValidationError`。

**原因**：`strict_clearance=True` 且 `lost_time` 缺少某个相邻相位对。

**处理**：补全 `lost_time`，或设置 `strict_clearance=False`（不建议用于正式结果）。

### 8.10 需求过高导致不可行

**症状**：第 1 轮直接不可行。

**排查**：

1. 每个流向的 `q_m / max_p a_pm` 是否接近或超过 1；
2. 关键流向流量比之和 + 清空时间占比是否超过 1；
3. 是否有流向没有被任何相位服务；
4. 是否 `c_max` 太小。

### 8.11 相位数过多/过少

- 相位数是第三目标，第一、第二目标优先；
- 如果希望严格限制相位数，加 `Σy` 上界约束；
- 如果希望某相位必选，加 `y_p ≥ 1`。

### 8.12 候选相位物理冲突

框架只做数学模型，**不检查交通冲突**。如果相位池中包含互相冲突的流向组合，模型可能给出物理上不可行的方案。候选相位池必须由用户保证冲突安全。

### 8.13 零需求流向

当前 `Movement` 要求 `demand > 0`。如果某流向需求为 0，可以：

- 不放入 `movements`；
- 或给一个极小正值，但不推荐。

### 8.14 数值容差误判

如果后验校验失败但违反量在 `1e-5` 附近，优先检查系数尺度和 big-M，而不是直接修改容差。

---

## 9. 可视化与结果解读

### 9.1 总览甘特图

```python
from signal_timing.plotting import plot_signal_timing_gantt

plot_signal_timing_gantt(data, result, cycles=2, save_path="gantt.png")
```

包含：

- 相位配时甘特图；
- 累计服务能力 vs 需求；
- 周期末需求满足柱状图。

### 9.2 新版流向甘特图（论文风格）

```python
from signal_timing.plotting import (
    plot_movement_release_gantt,
    compute_satisfaction_times,
)

sat = compute_satisfaction_times(data, result)

plot_movement_release_gantt(
    data,
    result,
    cycles=1,
    save_path="movement_gantt.png",
    show=False,
)
```

表达：

- 每个“进口 x 流向”一条泳道；
- 相位放行区间画柱子；
- `t_sat` 前实心，`t_sat` 后细密斜条纹；
- 浅灰竖带 = 清空/全红时间，顶部标时长；
- 每条泳道在 `t_sat` 处画同色竖虚线；
- y 轴标签给出总绿灯时长和 `t_sat`；
- 柱子内部标注实心/斜条纹持续时间；
- 背景有淡灰横向泳道分隔线和较密纵向网格；
- 默认只画一个周期。

### 9.3 需求满足时刻

```python
from signal_timing.plotting import satisfaction_time, build_schedule

plan = build_schedule(data, result)
t_sat = satisfaction_time(data, plan, "N_left")
```

定义：

```text
t_sat = 从周期起点开始累计服务能力首次达到 q_m · C / 3600 的时间
```

若一个周期内无法满足，返回 `None`。

---

## 10. 完整案例

### 10.1 两相位手算案例

```python
data = IntersectionData(
    movements={"E": Movement("E", 600), "W": Movement("W", 600)},
    phases={
        "P1": Phase("P1", {"E": 1800}),
        "P2": Phase("P2", {"W": 1800}),
    },
    lost_time={("P1","P2"): 5, ("P2","P1"): 5},
    g_min=15, c_min=30, c_max=180,
)

result = LexicographicOptimizer(data, mip_rel_gap=0.001).solve()
# C=40, selected=[P1,P2], greens={P1:15,P2:15}
```

### 10.2 四进口 x 两流向 + 搭接相位

运行：

```bash
/home/qktx/artery_milp/conda-envs/artery_milp/bin/python examples/four_approach_overlap_case.py
```

当前需求：

| 进口 | 直行 | 左转 |
|---|---:|---:|
| N | 650 | 240 |
| S | 430 | 170 |
| E | 470 | 150 |
| W | 350 | 110 |

含搭接相位结果：

```text
C = 115.541 s
selected = P1_NS_TH, P3_EW_TH, P4_EW_LT, P5_N_THLT, P6_S_THLT
order = P1_NS_TH → P5_N_THLT → P3_EW_TH → P4_EW_LT → P6_S_THLT
waste = 31581.08
```

纯对称相位对照：

```text
C = 135.849 s
selected = P1_NS_TH, P2_NS_LT, P3_EW_TH, P4_EW_LT
```

结论：加入南北直行左转搭接相位后周期降低约 14.9%。

### 10.3 给所有候选相位提供分层参考顺序

如果希望所有候选相位有一个默认的参考顺序，可以直接在 `IntersectionData`
上提供 `reference_order`。它支持**分层**：

- 一个元素代表一层；
- 同一层内的相位可以互换；
- 必须覆盖全部候选相位，且每个相位只能出现一次。

例如当前 4 进口案例，如果想按“南北组（直行 → 搭接 → 左转）→ 东西组（直行 → 搭接 → 左转）”组织，需要相位池中同时有东西搭接候选：

```python
# 在原有 P1~P6 基础上，补充东西搭接候选：
# P7_E_THLT = 东进口直行 + 左转
# P8_W_THLT = 西进口直行 + 左转

REFERENCE_ORDER = [
    # 南北组：直行 → 搭接 → 左转
    ("P1_NS_TH",),
    ("P5_N_THLT", "P6_S_THLT"),     # 南北搭接，层内可互换
    ("P2_NS_LT",),
    # 东西组：直行 → 搭接 → 左转
    ("P3_EW_TH",),
    ("P7_E_THLT", "P8_W_THLT"),     # 东西搭接，层内可互换
    ("P4_EW_LT",),
]

data = IntersectionData(
    movements=...,
    phases=...,                     # 包含 P1~P8
    lost_time=...,
    g_min=11.0, c_min=40.0, c_max=180.0,
    reference_order=REFERENCE_ORDER,
)
```

求解时通过 `reference_mode` 选择参考强度：

```python
# 1) hard：只允许与分层参考顺序一致的顺序
result = opt.solve(reference_mode="hard")

# 2) prefer / soft：全枚举；主目标在容差内时，参考顺序做次级偏好
result = opt.solve(reference_mode="prefer", reference_tolerance=1e-3)

# 3) none：忽略参考顺序
result = opt.solve(reference_mode="none")
```

三种模式的含义：

| 模式 | 行为 |
|---|---|
| `hard` | 只枚举层内排列，所有候选顺序必须满足层级先后；层内选浪费最小的 |
| `prefer` / `soft` | 枚举全部顺序；若某顺序的浪费在 `reference_tolerance` 容差内，则优先选层级逆序更少的 |
| `none` | 忽略参考顺序，退化为原始全枚举 |

当前案例（强制选中 `P2_NS_LT`，`reference_mode="hard"`）：

```text
最终周期 C = 115.541 s
第一阶段保守 C* = 152.027 s
选中相位 = P1_NS_TH, P2_NS_LT, P3_EW_TH, P4_EW_LT, P5_N_THLT
相位顺序 = P1_NS_TH → P5_N_THLT → P2_NS_LT → P3_EW_TH → P4_EW_LT
```

说明：

- 南北组：`P1_NS_TH`（直行）→ `P5_N_THLT`（搭接）→ `P2_NS_LT`（左转）；
- 东西组：`P3_EW_TH`（直行）→ `P4_EW_LT`（左转）；
- `P6_S_THLT`、`P7_E_THLT`、`P8_W_THLT` 在当前最优解中没有被选中，因此相应搭接层为空；
- 东西方向即使补充了 `P7/P8` 搭接候选，最优解仍然选择 `P3_EW_TH → P4_EW_LT`，周期保持 115.541s；
- `hard` 模式严格按层级枚举；`prefer/soft` 模式允许在浪费目标容差内用层级逆序做次级偏好；
- 更复杂的顺序规则仍然可以继续使用 `order_filter`；
- `make_reference_order_filter(reference_order)` 可以把分层参考顺序直接转成 `order_filter`。

完整示例见 `examples/four_approach_sequence_case.py`。


### 10.4 额外绿灯规则案例：如何避免等比缩放

在“南北组 → 东西组”顺序案例基础上，增加三条规则：

1. 所有被选中相位绿灯 >= 15s；
2. `P5_N_THLT + P2_NS_LT >= 32s`；
3. `P5_N_THLT - P2_NS_LT >= 5s`（非齐次约束，用于打破统一最小绿导致的等比缩放）。

实现：

```python
# 规则 1：所有选中相位绿灯 >= 15s
data.g_min = 15.0

opt = LexicographicOptimizer(data, mip_rel_gap=0.001)

# 强制选中 P2 和 P5，使规则 2、3 都有意义
opt.add_constraint(ConstraintSpec(
    name="force_P2_NS_LT",
    coeffs={("y", "P2_NS_LT"): 1.0},
    sense=">=", rhs=1.0,
))
opt.add_constraint(ConstraintSpec(
    name="force_P5_N_THLT",
    coeffs={("y", "P5_N_THLT"): 1.0},
    sense=">=", rhs=1.0,
))

# 规则 2：P5_N_THLT + P2_NS_LT >= 32s
opt.add_constraint(ConstraintSpec(
    name="P5_plus_P2_min_32",
    coeffs={("g", "P5_N_THLT"): 1.0, ("g", "P2_NS_LT"): 1.0},
    sense=">=", rhs=32.0,
    trigger=Trigger.all_of("P5_N_THLT", "P2_NS_LT"),
))

# 规则 3：P5_N_THLT - P2_NS_LT >= 5s（非齐次，打破缩放）
opt.add_constraint(ConstraintSpec(
    name="P5_minus_P2_ge_5",
    coeffs={("g", "P5_N_THLT"): 1.0, ("g", "P2_NS_LT"): -1.0},
    sense=">=", rhs=5.0,
    trigger=Trigger.all_of("P5_N_THLT", "P2_NS_LT"),
))

result = opt.solve(reference_mode="hard")
```

结果：

```text
最终周期 C = 150.000 s
第一阶段 C* = 173.077 s
选中相位 = P1_NS_TH, P2_NS_LT, P3_EW_TH, P4_EW_LT, P5_N_THLT
相位顺序 = P1_NS_TH → P5_N_THLT → P2_NS_LT → P3_EW_TH → P4_EW_LT

P1_NS_TH   35.833 s
P5_N_THLT  22.000 s
P2_NS_LT   17.000 s
P3_EW_TH   39.167 s
P4_EW_LT   15.000 s

最小选中绿灯 = 15.000 s
P5_N_THLT + P2_NS_LT = 39.000 s
P5_N_THLT - P2_NS_LT = 5.000 s
```

### 为什么必须加规则 3

如果只把 `g_min` 从 11 提高到 15，模型会倾向于把原最优解整体等比放大：

```text
原解最小绿灯 P4_EW_LT = 11.554s
λ = 15 / 11.554 ≈ 1.2982
```

由于服务能力约束、环约束和目标函数基本是关于 `(g, C)` 的一次齐次式，把原解整体乘以 `λ` 后：

- 服务能力自动满足；
- 最小绿灯正好达到 15s；
- `P5+P2` 从 27.216s 被动放大到 35.333s，超过 32s；
- 所以 `P5+P2>=32` 这条规则在缩放解上并不紧，结果看起来就是原方案等比放大。

加入 `P5 - P2 >= 5` 后，约束含绝对差值，整体缩放不再保持可行，因此结果不再是原解的比例放大。

### 对照实验

| 场景 | 周期 | P5 | P2 | P5+P2 | 最小绿灯 |
|---|---:|---:|---:|---:|---:|
| 原始（g_min=11，无额外规则） | 115.541s | 14.122 | 13.095 | 27.216 | 11.554 |
| 仅 `g_min=15` | 150.000s | 18.333 | 17.000 | 35.333 | 15.000 |
| `g_min=15` + `P5+P2>=32` | 150.000s | 18.333 | 17.000 | 35.333 | 15.000 |
| `g_min=15` + `P5+P2>=32` + `P5-P2>=5` | 150.000s | 22.000 | 17.000 | 39.000 | 15.000 |
| `g_min=15` + `P5+P2>=36` | 152.500s | 18.639 | 17.361 | 36.000 | 15.250 |
| `g_min=15` + `P5+P2>=40` | 162.500s | 19.861 | 20.139 | 40.000 | 16.250 |

结论：

- “等比放大”不是求解器错误，而是线性齐次模型的正常性质；
- 如果希望 `P5+P2>=32` 真正成为紧约束，可以把阈值提高到 36s 或 40s；
- 如果希望保留 32s 阈值但得到非等比分配，可以加入 `P5-P2>=5` 这样的非齐次约束；
- 当前案例中加入该约束后，周期仍为 150s，但 `P5` 绿灯从 18.333s 增加到 22s，`P2` 保持 17s。

完整示例见 `examples/four_approach_strict_green_case.py`。

---

## 11. 测试与复现

运行完整测试：

```bash
cd /mnt/e/PythonProjects/intersection_milp
/home/qktx/artery_milp/conda-envs/artery_milp/bin/python -m unittest discover -s tests -t . -v
```

当前测试共 61 个，覆盖：

- 数据校验；
- Trigger / ConstraintSpec；
- big-M、触发矩阵、软约束、触发坍缩；
- 子集审计；
- 字典序管线；
- 排序后处理；
- 复杂 4 进口案例；
- 甘特图与需求满足时刻。

---

## 12. 扩展点与已知限制

### 已支持

- 相位选择 + 配时；
- 通用线性约束；
- ALWAYS/OR/AND 触发；
- 软约束；
- 子集审计；
- 字典序多目标；
- 两阶段排序；
- 固定周期/自由周期；
- no-good cut 回退；
- 后验校验；
- 论文风格甘特图。

### 已知限制

- 只支持单交叉口，不支持干线协调/绿波；
- 顺序枚举是 n!，候选相位数不宜过大；
- 不检查交通冲突，候选相位池必须物理可行；
- 不显式建模行人、非机动车专用相位；
- 不支持随机需求/鲁棒优化；
- 不支持 HiGHS indicator constraints，全部用 big-M 表达；
- 求解结果可能因 HiGHS 版本不同而变化；
- 零需求流向当前不支持；
- 不提供文件级日志落盘，但 `result.rounds` 保留各轮轨迹。

### 可能的扩展

| 扩展 | 接入位置 |
|---|---|
| 饱和度约束 | 服务能力约束加系数 |
| 候选相位池自动生成 | 数据层之前 |
| k-of-n 触发 | Trigger 增加类型 + 辅助二进制 |
| 绿波/干线协调 | 破坏单路口假设，需重构 |
| 随机需求/鲁棒优化 | 数据层扩展场景集 |
| 多周期/过渡优化 | 目标管线和排序层扩展 |

---

## 13. 附录：符号表与 API 速查

### 符号表

| 符号 | 含义 |
|---|---|
| `q_m` | 流向 m 的需求流量 |
| `a_pm` | 相位 p 对流向 m 的服务能力 |
| `y_p` | 相位 p 是否选中 |
| `g_p` | 相位 p 绿灯时长 |
| `C` | 周期 |
| `ℓ(i,j)` / `l_bar` | 相邻清空时间 / 最大清空时间 |
| `S` | 触发涉及的相位集合 |
| `M⁺` / `M⁻` | ≤ / ≥ 型 big-M |
| `σ` | 软约束松弛 |
| `eps_cycle` / `eps_waste` / `delta_abs` | 字典序锁定容差 |
| `t_sat` | 流向需求满足时刻 |

### API 速查

```python
# 数据
IntersectionData(movements, phases, lost_time, g_min, c_min, c_max)

# 约束
Trigger.always()
Trigger.any_of(*phases)
Trigger.all_of(*phases)

ConstraintSpec(name, coeffs, sense, rhs, trigger, soft, penalty, slack_max)

# 优化器
opt = LexicographicOptimizer(data, eps_cycle=0.01, eps_waste=0.01,
                             delta_abs=None, mip_rel_gap=0.005,
                             time_limit=300.0, disp=False, max_fallback=20)

opt.add_constraint(spec)
opt.add_constraints([spec1, spec2])
opt.audit(spec)
result = opt.solve(fixed_cycle=None, order_filter=None,
                   run_ordering=True, allow_cycle_reduction=True)

# 结果
result.cycle
result.selected
result.greens
result.order
result.waste
result.sigmas
result.rounds
result.fallback_cuts
result.verification

# 可视化
plot_signal_timing_gantt(data, result, cycles=2, save_path=...)
plot_movement_release_gantt(data, result, cycles=1, save_path=...)
compute_satisfaction_times(data, result)
satisfaction_time(data, plan, mid)
```

---

**文档结束。**
